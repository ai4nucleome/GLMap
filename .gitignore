tools/
# Secrets and credentials
.env
*.env
.pypirc
*token*.txt
*api_key*

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyTorch / large model weights
*.pth
*.pt
*.bin

# Transformers / HF caches
.cache/
transformers_cache/
.huggingface/
/hf

# Virtual environments
.venv
env/
venv/
ENV/
env.bak/
venv.bak/
.python-version

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# pytest / coverage
.pytest_cache/
.coverage
htmlcov/

# Jupyter
.ipynb_checkpoints/

# Logs
*.log
logs/
scripts/logs/

# OS
.DS_Store
Thumbs.db

# Project-specific: large raw benchmark corpora the user downloads
# (see data/README.md) — never committed
data/GUE/
data/PGB/
data/dna_foundation_benchmark/
data/audits/tokenization_calibration.csv

# Local non-HF model weights (megaDNA / PlasmidGPT / GenSLM / evo .pt etc.)
models/modelsHFNoInfo/

# Regenerable bulk outputs. The rest of results/ IS tracked (scored
# matrices + per-model parquets) so the figures reproduce; only the heavy
# per-model × per-task embeddings (step 4) are left out.
results/analysis/embeddings/

# Internal / Claude Code agent state
.claude/
.codex/
.agents/
CLAUDE.md
